apiversion: v1
kind: Service
metadata:
  name: flask-app-service
  labels:
    app: flask-app
spec:
  type: LoadBalancer
  selector:
    app: flask-app
  ports:
  - protocol: TCP 
    port: 80
    targetPort: 5000
  - protocol: TCP
    port: 443
    targetPort: 5000
    
